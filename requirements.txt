# ── Core UI ───────────────────────────────────────────────────────────────────
customtkinter>=5.2.2
pillow>=10.0.0

# ── Web framework (local Web UI) ──────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── HTTP / async networking ───────────────────────────────────────────────────
requests>=2.31.0
aiohttp>=3.9.0

# ── Ollama / LLM ──────────────────────────────────────────────────────────────
ollama>=0.1.6
langchain-ollama>=0.1.0

# ── System / utilities ────────────────────────────────────────────────────────
psutil>=5.9.0
packaging>=23.0

# ── ML / datasets (Mental State Monitor pipeline) ────────────────────────────
datasets>=2.18.0           # download psychology datasets — run: python build_datasets.py

# ── Memory / voice — /lo semantic recall and speech both hard-depend on these.
# They were listed as optional, so a clean venv rebuild silently dropped them:
# chromadb missing makes _lo_rag_recall() return "" with no error, and /lo just
# quietly forgets things. Not optional.
chromadb>=0.4.22           # vector memory — /lo semantic recall, /analyze retrieval
faster-whisper>=1.0.0      # speech-to-text — voice input and call mode

# ── Optional — enable extra features ─────────────────────────────────────────
# pdfplumber             # import PDF files into sessions
# spacy                  # NLP / language detection
# nltk                   # tokenization
# language_tool_python   # grammar check (also needs Java 8+)
# pygame                 # cross-platform audio (dragon sounds)
# playsound              # lightweight audio fallback
