# ── Core UI ───────────────────────────────────────────────────────────────────
customtkinter>=5.2.2
pillow>=10.0.0

# ── Web framework (local Web UI) ──────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── HTTP / async networking ───────────────────────────────────────────────────
requests>=2.31.0
aiohttp>=3.9.0

# ── Ollama / LLM ──────────────────────────────────────────────────────────────
ollama>=0.1.6
langchain-ollama>=0.1.0

# ── Arabic / RTL rendering ────────────────────────────────────────────────────
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# ── System / utilities ────────────────────────────────────────────────────────
psutil>=5.9.0
packaging>=23.0

# ── Optional — enable extra features ─────────────────────────────────────────
# chromadb>=0.4.22       # vector memory / RAG storage
# pdfplumber             # import PDF files into sessions
# spacy                  # NLP / language detection
# nltk                   # tokenization
# language_tool_python   # grammar check (also needs Java 8+)
# pygame                 # cross-platform audio (dragon sounds)
# playsound              # lightweight audio fallback
